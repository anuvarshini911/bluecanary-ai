apiVersion: v1
kind: Service
metadata:
  name: payment-gateway-active
  labels:
    app: payment-gateway
spec:
  selector:
    app: payment-gateway
  ports:
    - port: 80
      targetPort: 8000
      name: http
---
apiVersion: v1
kind: Service
metadata:
  name: payment-gateway-preview
  labels:
    app: payment-gateway
spec:
  selector:
    app: payment-gateway
  ports:
    - port: 80
      targetPort: 8000
      name: http
